import { shallowMount, mount } from '@vue/test-utils'
import PreviousEvents from "@/views/PreviousEvents.vue";

// Should when created dispatch a action to vuex store
// Get data from the Vuex store (should be array with previous events)

 describe('PreviousEvents.vue', () => {

    it("should have child component (Event) that is going to render events", () => {

    });
    

})