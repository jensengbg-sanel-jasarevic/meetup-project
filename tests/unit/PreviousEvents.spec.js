import { shallowMount } from '@vue/test-utils'
import { mockDataObj } from "./mockData"
import PreviousEvents from '@/components/PreviousEvents.vue'

// should have a element with class '.event-box' if there is data in the component
// should not have element with class '.event-box' if there is no data in the component
// should when component is mounted have one component with name "Review"
// should display correct data (for event, city, date etc) when component mounted

describe('PreviousEvents.vue', () => {

}) 